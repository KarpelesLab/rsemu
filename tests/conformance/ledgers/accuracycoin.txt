# Known failures: AccuracyCoin (125 whole-machine tests, MIT, (c) 2025 Chris Siebert).
#
# Keyed by the low byte of the test's result address rather than by name: the
# address is what the ROM writes and is stable across releases, whereas the
# display name is text on a menu. tests/conformance/accuracycoin_tests.rs has
# the mapping, and the runner prints both.
#
#   4d                # $044D, "PC Wraparound"
#
# The `# why` note is mandatory, and this file only ever shrinks.
#
# Two caveats worth knowing before anything is written down here:
#
#   * The ROM targets an RP2A03G CPU and an RP2C02G PPU. A handful of tests
#     legitimately behave differently on other revisions — a divergence must be
#     traced to a documented revision difference before it is excused, not
#     assumed to be one.
#   * The five "DRAW" pages assert nothing and are excluded by the runner, so
#     they can never appear here.
#
# ---------------------------------------------------------------------------
# Measured 2026-08-31 against the pinned ROM: 85 of 141 pass, 56 fail.
# ---------------------------------------------------------------------------
#
# The earlier "81 of 125" was measured through a stale copy of the ROM's
# `TestPages:` table. The pinned ROM carries 146 menu entries — 141 that assert
# and 5 DRAW pages sharing `result_DrawTest` — and the old table named three
# addresses ($0402, $0418, $0464) that ROM never writes while omitting nineteen
# tests it does run. `accuracycoin_tests.rs` has been regenerated from the ROM;
# of the nineteen recovered tests four already pass and fifteen are below.
#
# Every entry below names a subsystem rsemu has not built yet, and each one is
# traced to the specific error code the ROM reported and the line in the ROM's
# own README that explains it. There are no "fails and I do not know why"
# entries; if one appears, it is a bug and not a ledger line.
#
# Five causes account for all 44:
#
#   A. No /RDY on cpu.mos6502, so no DMA can halt the CPU.
#   B. No DMC DMA at all — the APU's sample fetch is a CPU-side stall that
#      needs (A) first.
#   C. No CPU-side open-bus data latch: the 6502 does not remember the last
#      byte it drove, so reads of write-only addresses do not float.
#   D. PPU staleness is scanline-exact, not dot-exact (ROADMAP.md 4.2's one
#      remaining phase-3 item).
#   E. APU frame-counter and length-counter edges are not on the right CPU
#      cycle, and neither the APU nor the ports can see get/put phase.
#
# ---------------------------------------------------------------------------
# A. The CPU has no /RDY line, so nothing can halt it
# ---------------------------------------------------------------------------
#
# nes.oamdma performs the whole transfer correctly and records the 513 cycles
# it should have stalled for (`OamDma::stall_owed`), but cpu.mos6502 has no pin
# and no hook to be handed them. src/dev/nes/dma.rs names the two shapes the
# seam could take. Everything here is that one missing line.

46                # $0446 "$93 SHA indirect,Y" code 7: with /RDY low 2 cycles
                  # before the write, the target address is wrong. Codes 0-6 —
                  # the instruction itself — all pass.
47                # $0447 "$9F SHA absolute,Y", the same code 7, the same /RDY.
48                # $0448 "$9B SHS absolute,Y", the same code 7, the same /RDY.
49                # $0449 "$9C SHY absolute,X", the same code 7, the same /RDY.
4a                # $044A "$9E SHX absolute,Y", the same code 7, the same /RDY.

# ---------------------------------------------------------------------------
# B. No DMC DMA
# ---------------------------------------------------------------------------
#
# The APU models the DMC channel but not its sample fetch, which on hardware is
# a DMA that steals CPU cycles — so it needs (A) before it can exist at all.
# Every test on the ROM's "APU Registers and DMA tests" page is about where that
# fetch lands relative to a CPU access, so all of them report the same thing.

6c                # $046C "DMA + Open Bus" code 1: LDA $4000 reads back $00
                  # rather than floating. Needs (C) as well as the DMC DMA.
4c                # $044C "DMA + $2007 Read" code 2: the DMC DMA was on the
                  # wrong cycle, or its halt cycles did not read $2007.
4f                # $044F "DMA + $2007 Write" code 1: its own prerequisite,
                  # $044C above, did not pass.
5d                # $045D "DMA + $4015 Read" code 2: the halt cycles did not
                  # read $4015 and so did not clear the frame IRQ flag.
5e                # $045E "DMA + $4016 Read" code 1: the halt cycles did not
                  # read $4016, so the controller was not clocked by the DMA.
6b                # $046B "DMC DMA Bus Conflicts" code 2: no DMC DMA, so no bus
                  # conflict with the APU registers to emulate.
77                # $0477 "DMC DMA + OAM DMA" code 1: DMC DMA timing. There is
                  # no DMC DMA to overlap the OAM one with.
79                # $0479 "Explicit DMA Abort" code 1: as above.
78                # $0478 "Implicit DMA Abort" code 1: as above.
5c                # $045C "APU Register Activation" code 1: the ROM says
                  # outright that a series of prerequisites failed — CPU and PPU
                  # open bus, DMA + Open Bus, DMA + $2007 Read. Cascades from
                  # (B) and (C); nothing of its own.
61                # $0461 "Interrupt flag latency" code 1: an IRQ should occur
                  # when a DMC sample ends with the DMC IRQ enabled. Without the
                  # sample fetch the sample never ends.

# ---------------------------------------------------------------------------
# C. No CPU-side open-bus data latch
# ---------------------------------------------------------------------------
#
# The address space has an open-bus policy per space (`unassigned = open-bus`),
# which is what makes an unmapped address answer. What is missing is the *CPU's*
# side: a 6502 leaves the last byte it drove on the data bus, and a read of a
# write-only register returns that byte rather than anything the device chose.
# That latch belongs in cpu.mos6502, beside the cycle accounting.

08                # $0408 "Open Bus" code 2: LDA absolute from open bus should
                  # return the high byte of its own operand.
7c                # $047C "JSR Edge Cases" code 3: the ROM says "incorrect open
                  # bus emulation". Codes 1, 2 and 4 — the push order and the
                  # operand fetch — pass.
60                # $0460 "Instruction Timing" code 1: "the DMA should update
                  # the data bus". Every cycle count from code 3 on is untested
                  # until this passes; it is not a timing failure.
6d                # $046D "Implied Dummy Reads" code 3: the data bus was not
                  # updated when the DMC DMA occurred. (B) and (C) together.

# ---------------------------------------------------------------------------
# D. PPU staleness is scanline-exact, not dot-exact
# ---------------------------------------------------------------------------
#
# ROADMAP.md 4.2 names this as the one thing left in phase 3: the run loop stops
# the CPU at the PPU's next *event*, which puts the vblank flag and the NMI on
# the right scanline but not always on the right dot. Every test below reports
# a flag set or cleared on the wrong PPU cycle, which is exactly that.

50                # $0450 "VBlank beginning" code 1: the $2002 vblank flag was
                  # not set on the correct PPU cycle.
51                # $0451 "VBlank end" code 1: and not cleared on the correct
                  # one either.
52                # $0452 "NMI Control" code 3: an NMI enabled during vblank
                  # while the vblank flag is set must fire immediately.
53                # $0453 "NMI Timing" code 1: the NMI did not occur on the
                  # correct PPU cycle.
54                # $0454 "NMI Suppression" code 1: a precisely timed $2002 read
                  # must suppress the NMI, which needs the dot to be exact.
55                # $0455 "NMI at VBlank end" code 1: the NMI was disabled too
                  # early or occurred too late.
56                # $0456 "NMI disabled at VBlank" code 1: as above.
57                # $0457 "Sprite 0 Hit behavior" code E: the hit flag was set
                  # too early. Codes 1 through D — every behavioural case,
                  # masks, edges and transparency — pass; only the dot is wrong.

# ---------------------------------------------------------------------------
# D2. PPU sprite-evaluation quirks not modelled
# ---------------------------------------------------------------------------
#
# Not timing: these are behaviours of the two-phase sprite evaluation and of
# $2004 that the engine does not implement yet. Each is a piece of work in
# src/dev/ppu, not a seam.

58                # $0458 "Arbitrary Sprite zero" code 2: the first sprite
                  # *processed* on a scanline is the one that can produce a
                  # sprite-zero hit, not OAM entry 0.
5a                # $045A "Misaligned OAM behavior" code 1: a non-zero OAMADDR
                  # at the start of a scanline must stay misaligned through
                  # evaluation and still be able to hit.
5b                # $045B "Address $2004 behavior" code 4: a read of $2004
                  # during dots 1-64 of a visible scanline with rendering on
                  # must return $FF, because secondary OAM is being cleared.
7b                # $047B "OAM Corruption" code 2: the documented corruption
                  # that copies OAM row 0 over another row when rendering is
                  # switched off mid-frame is not modelled.

# ---------------------------------------------------------------------------
# E. APU frame counter, and get/put phase
# ---------------------------------------------------------------------------
#
# The frame counter's four edges are not placed on exact CPU cycles — `nes.apu`
# reports `next_event_tick() == None` for precisely this reason — and neither
# the APU nor the controller ports can see whether an access lands on a "get" or
# a "put" cycle, because a device is handed a bus access, not a CPU half-cycle.

65                # $0465 "Length Counter" code 4: writing $80 to $4017 must
                  # clock the length counter immediately.
66                # $0466 "Length Table" code 1: its prerequisite, $0465, did
                  # not pass. The table itself is untested until then.
67                # $0467 "Frame Counter IRQ" code 7: the IRQ flag must not be
                  # cleared yet on a get -> put transition.
68                # $0468 "Frame Counter 4-step" code 2: the first clock of the
                  # length counters was late.
69                # $0469 "Frame Counter 5-step" code 1: the first clock of the
                  # length counters was early.
6a                # $046A "Delta Modulation Channel" code 1: $4015 bit 4 must
                  # follow a playing sample. See (B) — there is no sample fetch.
5f                # $045F "Controller Strobing" code 4: the controllers must not
                  # be strobed on a put -> get transition. Codes 1-3 pass, so
                  # the latch and the shift register are right and only the
                  # cycle phase is invisible to the device. "Controller
                  # Clocking" ($047A) passes outright.
62                # $0462 "NMI Overlap BRK" code 2: interrupt hijacking — an NMI
                  # arriving during BRK's vector fetch. The core polls
                  # interrupts per instruction, not per cycle.
63                # $0463 "NMI Overlap IRQ" code 1: the same hijacking, for IRQ.


# ---------------------------------------------------------------------------
# G. Recovered by regenerating the table — measured for the first time
# ---------------------------------------------------------------------------
#
# Nineteen tests the stale table omitted. Four pass ("CHR ROM is not writable",
# "Attributes As Tiles", "t Register Quirks", "Branch Dummy Reads"); these
# fifteen do not. Each entry names the error code the ROM reported and the
# subsystem it belongs to, from the README's own tables.

88                # $0488 "DMA + $2002 Read" code 2: the DMC DMA's halt and
                  # alignment cycles must read $2002. Cause (A)/(B): no /RDY,
                  # no DMC DMA.
86                # $0486 "Rendering Flag Behavior" code 2: what $2002 bit 6 and
                  # the rendering enable do across the pre-render line, at dot
                  # resolution. Cause (D).
8a                # $048A "$2007 read w/ rendering" code 2: a $2007 read while
                  # rendering does the two-increment v bump instead of the
                  # buffered read. Not modelled.
8d                # $048D "$2002 flag timing" code 1: the sprite-0/overflow
                  # flags must clear on an exact dot. Cause (D).
89                # $0489 "Suddenly Resize Sprite" code 2: changing $2000 sprite
                  # height mid-scanline, which the evaluator does not see.
80                # $0480 "INC $4014" code 1: a read-modify-write on $4014 does
                  # the write twice and so starts two OAM DMAs. Cause (A).
83                # $0483 "Stale BG Shift Registers" code 3: the background
                  # shift registers keep their contents when rendering is
                  # switched off; rsemu's engine has no shift registers.
8f                # $048F "Stale Sprite Shift Regs" code 3: as above, for the
                  # eight sprite shifters.
87                # $0487 "BG Serial In" code 2: the serial input to the
                  # background shifters. Same missing pipeline.
84                # $0484 "Sprites On Scanline 0" code 2: sprite evaluation on
                  # the first visible scanline. Cause (D2).
8c                # $048C "$2004 Stress Test" code 2: $2004 accesses on every
                  # dot of a rendered scanline. Needs (D) and (D2).
8e                # $048E "$2007 Stress Test" code 2: the same for $2007 and the
                  # v register's during-render increments.
91                # $0491 "ALE + Read" code 2: the PPU's address-latch-enable
                  # behaviour on the pattern bus. Not modelled.
92                # $0492 "Hybrid Addresses" code 2: the same bus, mixing a
                  # nametable and a pattern fetch. Not modelled.
90                # $0490 "Internal Data Bus" code 1: the CPU-side open-bus data
                  # latch. Cause (C).
