{
  "name": "rsemu-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "The rsemu browser demo: a Vue 3 shell around a hand-written wasm C ABI.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node check.mjs public/rsemu.wasm --site dist",
    "licenses": "node scripts/licenses.mjs"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "vite": "^6.0.0"
  }
}
